<!DOCTYPE html>
<html    <% if(locals.amp && amp) { %> amp <% } %>lang="en">
    <%- include head.ejs %>
    <body>
        <div class="site-wrapper">
            <!-- Header -->
            <%- include header.ejs %>
            <!-- /Header -->

            <!-- Content -->
            <%- include(page) %>
            <!-- /Content -->

            
            <!-- Footer -->
            <%- include footer.ejs %>
            <!-- /Footer -->
            <!-- Toggle for Darkmode and Lightmode-->
            <% if(!locals.amp || !amp) { %>
                <div class="theme-toggle" onclick="changeTheme()">🌓</div>
                <script type="text/javascript" src="/js/theme.js"></script>
                <link id="darkTheme" rel='stylesheet' href='/stylesheets/theme/dark.css' />
            <% } %>
            
        </div>

    </body>
</html>